# kit's own tasks — dogfooding. Run `kit build`, `kit test`.

build: compile kit into bin/
    go build -o bin/kit.exe ./cmd/kit

test: run every package test
    go test ./...

go-install: put kit on GOPATH/bin via `go install`
    go install ./cmd/kit
